<?xml version="1.0" encoding="utf-8"?>
<layout>
    <data>
        <variable
            name="headerNavigation"
            type="com.imtae.gitcheck.ui.MainActivity" />

            <import type="android.view.View"/>
    </data>

    <LinearLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/header_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="20dp"
        android:paddingBottom="10dp"
        android:orientation="vertical"
        android:background="@color/github_color"
        >

        <ImageView
            android:id="@+id/header_image"
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:layout_gravity="center"
            android:layout_marginBottom="10dp" />

        <TextView
            android:id="@+id/header_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingBottom="5dp"
            android:textSize="20dp"
            android:gravity="center"
            style="@style/white_font_style_bold"
            android:visibility="@{headerNavigation.userInfo.name == null  ? View.GONE : View.VISIBLE}"
            android:text="@{headerNavigation.userInfo.name}"/>

        <TextView
            android:id="@+id/header_nickname"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="15dp"
            android:gravity="center"
            style="@style/white_font_style_basic"
            android:text="@{headerNavigation.userInfo.login}"/>
    </LinearLayout>

</layout>